fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
SQLAlchemy>=2.0,<3.0
aiosqlite>=0.20,<1.0
httpx>=0.27,<1.0
websockets>=12.0
python-dotenv>=1.0,<2.0

# MetaTrader5 is Windows-only. It is intentionally NOT listed here so the
# backend installs cleanly on any OS; the MT5 adapter imports it lazily and
# reports a truthful OFFLINE status when the package/terminal is unavailable.
# On Windows, install it with:  pip install MetaTrader5

# Test dependencies
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
